# === Required ===
# Telegram Bot token from @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCDEF_your_real_token_here

# OpenAI API key (OpenAI SDK >= 1.x)
OPENAI_API_KEY=sk-your_openai_api_key

# === Optional ===
# Default chat model; can be changed at runtime with /model
OPENAI_MODEL=gpt-5-mini

# Comma-separated chat IDs to receive forwarded private DMs.
# Supports groups/supergroups (negative IDs like -100xxxxxxxxxxxx) and users.
# Example: forward to one group and one admin user
FORWARD_TO=-1001234567890123,123456789

# Timezone inside container/process (affects logs/timestamps only)
TZ=Asia/Shanghai

# Force unbuffered stdout/stderr for cleaner Docker logs
PYTHONUNBUFFERED=1

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# --- Advanced (optional; leave empty unless you know you need them) ---
# Custom OpenAI-compatible endpoint (proxy/self-host/Azure-style gateways)
# OPENAI_BASE_URL=https://api.openai.com/v1
# OpenAI organization or project scoping (if applicable)
# OPENAI_ORG=org_xxxxx
# OPENAI_PROJECT=proj_xxxxx